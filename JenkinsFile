pipeline {
    agent any

    environment {

    }

    stages {
        stage('Pull') {
            agent any
            
            steps {
                git 'https://github.com/huhu0327/MCInstall.git'
            }
        }

        // stage('Restore') {

        // }

        // stage('Test') {

        // }

        // stage('Analysis')

        // stage('Build') {

        // }

        // stage('Deploy') {

        // }
    }

}